# Setup
If you are not installing this package via composer you need to uncomment the require_once in openemr.boostrap.php for the vendor library.
You also will need to run in the module directory
```
composer install
```
